let add= function(num1,num2)
{
    return num1 + num2;
}
//The function is an expression which is assigned to add.
//the add is function and can call it passing parameters

let result = add(6,4)

console.log(result)
